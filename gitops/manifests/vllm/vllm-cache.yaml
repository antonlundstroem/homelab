apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vllm-cache
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      # HuggingFace weight cache. Disposable — re-downloads if lost.
      storage: 10Gi
