apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ollama-data
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      # GGUF model store (/root/.ollama). Disposable — models re-pull if lost.
      storage: 40Gi
