apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: open-webui-data
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      # Open WebUI SQLite DB + uploads. Clear manually on disk (see runbook).
      storage: 2Gi
